const TimeSeparatorState = Object.freeze({
  TIME_SEPARATOR_OFF: 0,
  TIME_SEPARATOR_ON: 1
});

const BlinkState = Object.freeze({
  BLINK_NONE: 0,
  BLINK_HOURS: 1,
  BLINK_MINUTES: 2
});

const AppMode = Object.freeze({
  CLOCK: 'clock',
  TETRIS: 'tetris',
  SNAKE: 'snake'
});
